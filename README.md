### Connaissance Git

```
Liste non exhausitive des commandes connues:
git init
git clone
git remote
git status
git add
git commit 
git push
git pull
git log
git branch
git merge
git checkout

Pret et motiver pour travailler sur de nouveaux projets 
```
